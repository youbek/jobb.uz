import styled from "styled-components";
import Container from "../Layout/Container";

const JobsFeedContainer = styled(Container)`
  display: block;
  min-height: calc(100vh - 260px);
`;
export default JobsFeedContainer;
