import styled from "styled-components";
import Container from "../Layout/Container";

const JobPageContainer = styled(Container)`
  min-height: calc(100vh - 260px);
  display: block;
`;

export default JobPageContainer;
