import styled from "styled-components";

const NavbarNav = styled.ul`
  display: flex;
  flex-direction: row;
  margin-left: auto;
  list-style: none;
  margin-bottom: 0;
  padding-left: 0;
`;

export default NavbarNav;
